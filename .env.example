# Required for both stdio and HTTP transports
LEDGERMEM_API_URL=https://api.proofly.dev
LEDGERMEM_API_KEY=
LEDGERMEM_WORKSPACE_ID=

# Optional — scopes all tool calls to a single actor
LEDGERMEM_ACTOR_ID=

# HTTP transport only
PORT=8787
